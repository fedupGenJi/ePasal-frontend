import React from 'react';

const AdminPage: React.FC = () => {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '3rem',
        fontWeight: 'bold',
      }}
    >
      admin
    </div>
  );
};

export default AdminPage;
